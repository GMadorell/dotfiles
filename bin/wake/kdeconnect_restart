#!/bin/bash
echo "`date`: RUNNING: kdeconnect restart"
pkill -x kdeconnectd
pkill -x "KDE Connect"
pkill -f "KDE Connect.app/Contents/MacOS/dbus-daemon"
sleep 15
nohup "/Applications/KDE Connect.app/Contents/MacOS/kdeconnectd" >/dev/null 2>&1 &
disown
echo "`date`: FINISHED: kdeconnect restart"
